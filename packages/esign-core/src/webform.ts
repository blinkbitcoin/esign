// @blinkbitcoin/esign-core/webform - the Apollo-free entry; an alias of
// ./docusign (the same surface, kept for the consumers that import it).
// Enforced Apollo-free by the webform-entry guard tests.

export * from './docusign';
