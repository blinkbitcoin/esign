apiVersion: v1
kind: Service
metadata:
  name: esign-service
  labels:
    app.kubernetes.io/name: esign-service
spec:
  selector:
    app.kubernetes.io/name: esign-service
  ports:
    - name: http
      port: 80
      targetPort: http
